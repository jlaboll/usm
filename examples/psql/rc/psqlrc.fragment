-- psql defaults contributed by the psql module. usm concatenates this into
-- ~/.psqlrc after a "generated by usm" header. Comment syntax here is psql's (--).
\set QUIET 1
\pset null '[null]'
\timing on
\set QUIET 0
