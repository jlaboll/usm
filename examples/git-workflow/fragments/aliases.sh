# git-workflow aliases. Sourced into interactive bash and zsh at shell startup.
# Keep fragments quiet (no echo) and cross-shell safe — aliases work identically.
alias gs='git status --short --branch'    # compact status with upstream tracking
alias gl='git log --oneline --graph -15'  # last 15 commits, one line each
